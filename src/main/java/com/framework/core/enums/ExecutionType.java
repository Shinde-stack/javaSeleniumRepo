package com.framework.core.enums;

/**
 * ExecutionType
 *
 * Placeholder for execution mode classification (local, CI, grid, cloud).
 *
 * Flow (planned):
 *   resolved at startup → drives driver factory, reporting paths, and retry policy
 *
 * Not referenced in the current lifecycle.
 */
public enum ExecutionType {

}
